import { StyleSheet } from "react-native";

export default StyleSheet.create({
  paginationContainer: {
    flexDirection: 'row',
    marginBottom: 40,
    marginTop: 20,
    gap: 20,
  },
  paginationButton: {
    padding: 10,
    backgroundColor: 'white',
    borderRadius: 5,
  },
})
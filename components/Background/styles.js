import { StyleSheet } from "react-native";

export default StyleSheet.create({
  bgImage: {
    width: "100%",
    height: "100%",
    position: "absolute",
    zIndex: -1,
    filter: "brightness(0.6)",
  }
});